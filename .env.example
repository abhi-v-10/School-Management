DEBUG=True
SECRET_KEY=replace-me

# Local defaults -> SQLite (no DB vars required)
# Uncomment to force SQLite everywhere (including prod) -- not recommended for production
# FORCE_SQLITE=True
# Allow using DATABASE_URL locally (otherwise ignored):
# LOCAL_USE_POSTGRES=1

# On Render ONLY (do not set locally unless testing Postgres):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=

# Google OAuth (sample placeholders)
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# Email (example SMTP config)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=School Management <no-reply@example.com>
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=example@gmail.com
# EMAIL_HOST_PASSWORD=app-password

WEBSOCKETS_ENABLED=1